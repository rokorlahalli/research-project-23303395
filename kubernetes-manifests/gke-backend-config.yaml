apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: backend-config
  namespace: app  # ← Your namespace
spec:
  timeoutSec: 180  # 3 minutes for Ollama responses
  connectionDraining:
    drainingTimeoutSec: 60
